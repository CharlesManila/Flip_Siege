# Rulebook copy for the play server

`rulebook.html` here is served when you run `python -m http.server` from `play/`.

**Canonical file:** `themes/flip-siege/rules/rulebook.html`

After editing the canonical rulebook, refresh this copy:

```powershell
Copy-Item -Force ..\..\rules\rulebook.html .\rulebook.html
```
